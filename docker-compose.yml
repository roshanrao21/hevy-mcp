services:
  hevy-mcp:
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      - "8000:8000"
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
